##
# Rhizome Bootstrap Configuration
##

# Set to false to disable persistence 
enable-persistence:  false
session-clustering-enabled: false
# Linker cannot be server as it is unable to deserialize tasks, in particular ones that have external dependencies that
# that are static and should be initialized by spring startup.
hazelcast:
    server: false
    group: "local"
    password: "trellis"
    seed-nodes: 
        - "localhost"

hikari:
    jdbcUrl: jdbc:postgresql://localhost:5432/openlattice
    #"jdbc:postgresql://wonderwoman.openlattice.com:30001,captainamerica.openlattice.com:30002/openlattice?ssl=true"
    #dataSourceClassName: "org.postgresql.ds.PGSimpleDataSource"
    username: "oltest"
    #username: "openlattice"
    password: "test"
    #dataSource.ssl: true
    #dataSource.sslMode: "verify-full"
    #dataSource.user: "mtamayo"
    #dataSource.databaseName: "openlattice"
    maximumPoolSize: 98
    #20 minutes
    connectionTimeout: 12000000

#graphite:
#    host: "jarvis"
#    global-prefix: "conductor"