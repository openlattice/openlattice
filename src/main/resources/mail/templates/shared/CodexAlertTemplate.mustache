<!doctype html>
<html>
  <head>
    <title>New Event</title>
    <style>

      @import url("//fonts.googleapis.com/css?family=Open+Sans:300,400,600,700,800");

      h1 {
        font-family: 'Open Sans', sans-serif;
        font-weight: 600;
        font-size: 18px;
        line-height: normal;
        color: #555e6f;
        text-transform: uppercase;
      }

      .header {
        font-size: 16px;
        text-transform: uppercase;
        line-height: normal;
        color: #8e929b;
      }

      body {
        font-family: 'Open Sans', sans-serif;
        line-height: 1.8em;
        color: #333;
      }

      .expiration {
        font-style: italic;
        font-size: 14px;
      }

      .vehicle {
        width: 100%;
        margin: 20px 0;
      }

      .divider {
        width: 100%;
        background-color: #e1e1eb;
        height: 1px;
        margin-bottom: 20px;
      }

      .readDetail {
        font-family: 'Open Sans', sans-serif;
        padding-top: 5px;
      }

      .hit {
        color: #ff3c5d;
      }

    </style>
  </head>

  <body>
    <div>
      <h1>New event for one of your subscriptions!</h1>
      <div class="divider" />
      <div class="header">Alert Details</div>

    </div>
  </body>
</html>
