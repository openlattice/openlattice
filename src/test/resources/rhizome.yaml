##
# Rhizome Bootstrap Configuration
##

# Set to false to disable persistence 
enable-persistence:  false
session-clustering-enabled: false

hazelcast:
    server: true
    group: "local"
    password: "trellis"
    seed-nodes: 
        - "localhost"
    cp-member-count: 0

postgres:
    citus: true
    initialize-tables: true
    initialize-indices: true
    hikari:
        jdbcUrl: jdbc:postgresql://localhost:5433/openlattice
        username: "oltest"
        password: "test"
        maximumPoolSize: 32
        connectionTimeout: 12000000 #20 minutes
